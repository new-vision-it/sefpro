class AppConfig {
  const AppConfig._();

  /// Placeholders for Firebase options. Replace with real values from Firebase console.
  static const firebaseOptionsReminder = 'Add firebase_options.dart via flutterfire configure.';

  /// Toggle to use mock repositories instead of Firebase.
  static const useMockData = false;
}
